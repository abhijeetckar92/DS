var assert = require('assert');

var Map = require('./maps');

//---------------------------------------  add(key,value) --------------------------------------//

describe('Map',()=>{
    describe('.add(key,value)',()=>{
        it('adds a new value to the collection for the given key',()=>{
            var originalMap = new Map();
            var expectedMap = originalMap.add()
        });
    });
});